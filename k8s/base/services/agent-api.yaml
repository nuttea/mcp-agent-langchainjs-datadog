apiVersion: v1
kind: Service
metadata:
  name: agent-api
  labels:
    app: mcp-agent
    service: agent-api
spec:
  selector:
    app: mcp-agent
    service: agent-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
