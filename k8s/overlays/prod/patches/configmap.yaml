apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Override PUBLIC_BASE_URL for prod environment
  PUBLIC_BASE_URL: "https://burger-api.platform-engineering-demo.dev"
  DD_ENV: "prod"
