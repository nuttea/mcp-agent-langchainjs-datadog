# IAP Architecture Diagram

## Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                           User Browser                               │
│                  https://www.platform-engineering-demo.dev           │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 │ 1. HTTP Request
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    Google Cloud IAP Layer                            │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ OAuth 2.0 Authentication Check                                │  │
│  │ - User not authenticated? → Redirect to Google Login          │  │
│  │ - User authenticated? → Check IAM permissions                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 │ 2. Request + IAP Headers
                                 │    ├─ X-Goog-Authenticated-User-Email
                                 │    ├─ X-Goog-Authenticated-User-Id
                                 │    ├─ X-Goog-IAP-JWT-Assertion
                                 │    └─ X-Forwarded-For
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│              GCP Application Load Balancer (Gateway API)             │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ Gateway: shared-gateway                                       │  │
│  │ HTTPRoute: mcp-agent-prod-route                              │  │
│  │ GCPBackendPolicy: agent-webapp-iap-policy                    │  │
│  │   ├─ IAP Enabled: true                                       │  │
│  │   ├─ OAuth Secret: oauth-client-secret                       │  │
│  │   └─ Client ID: 449012790678-o4n...                          │  │
│  └───────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 │ 3. Route to Backend
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      agent-webapp Service                            │
│                         (React Frontend)                             │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ - Serves static React application                            │  │
│  │ - Proxies API requests to agent-api                          │  │
│  │ - IAP headers pass through transparently                     │  │
│  └───────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 │ 4. API Calls
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       agent-api Service                              │
│                    (Node.js Express Backend)                         │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ IAP Middleware (iap-auth.ts)                                 │  │
│  │ ┌─────────────────────────────────────────────────────────┐  │  │
│  │ │ if (isIAPEnabled()) {                                    │  │  │
│  │ │   extractIAPUser(req, res, next)                         │  │  │
│  │ │     ├─ Extract email from X-Goog-Authenticated-User-Email│  │  │
│  │ │     ├─ Extract userId from X-Goog-Authenticated-User-Id  │  │  │
│  │ │     ├─ Attach user to request: req.iapUser              │  │  │
│  │ │     └─ Add Datadog tags:                                │  │  │
│  │ │         ├─ iap.user.email                                │  │  │
│  │ │         ├─ iap.user.id                                   │  │  │
│  │ │         ├─ usr.email                                     │  │  │
│  │ │         └─ usr.id                                        │  │  │
│  │ │ }                                                         │  │  │
│  │ └─────────────────────────────────────────────────────────┘  │  │
│  │                                                               │  │
│  │ Express Routes                                                │  │
│  │ ┌─────────────────────────────────────────────────────────┐  │  │
│  │ │ GET /api/me                                              │  │  │
│  │ │   ├─ Check IAP user: getIAPUser(req)                    │  │  │
│  │ │   ├─ Use iapUser.email as authenticationUserId          │  │  │
│  │ │   └─ Create/retrieve user from database                 │  │  │
│  │ │                                                           │  │  │
│  │ │ POST /api/chat                                           │  │  │
│  │ │   ├─ Get userId from IAP context                        │  │  │
│  │ │   ├─ Load chat history for user                         │  │  │
│  │ │   └─ Stream response with user context                  │  │  │
│  │ └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 │ 5. Store User Context
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        PostgreSQL Database                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ Tables:                                                       │  │
│  │ - users (id, created_at)                                     │  │
│  │ - conversations (id, user_id, title, created_at)            │  │
│  │ - messages (id, conversation_id, role, content, timestamp)  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘

                     ⇅ (Traces sent continuously)

┌─────────────────────────────────────────────────────────────────────┐
│                    Datadog Observability Platform                    │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ APM Traces with Custom Tags                                  │  │
│  │ ┌─────────────────────────────────────────────────────────┐  │  │
│  │ │ Span: express.request                                    │  │  │
│  │ │   service: agent-api                                     │  │  │
│  │ │   resource: POST /api/chat                               │  │  │
│  │ │   tags:                                                  │  │  │
│  │ │     - iap.user.email: user@example.com                  │  │  │
│  │ │     - iap.user.id: 123456789                            │  │  │
│  │ │     - usr.email: user@example.com                       │  │  │
│  │ │     - usr.id: 123456789                                 │  │  │
│  │ │     - http.url: /api/chat                               │  │  │
│  │ │     - http.status_code: 200                             │  │  │
│  │ └─────────────────────────────────────────────────────────┘  │  │
│  │                                                               │  │
│  │ DBM Queries with APM Correlation                             │  │
│  │ ┌─────────────────────────────────────────────────────────┐  │  │
│  │ │ SQL: SELECT * FROM conversations WHERE user_id = $1     │  │  │
│  │ │   - Linked to trace: iap.user.email                     │  │  │
│  │ │   - Query duration: 2.5ms                               │  │  │
│  │ └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

## Configuration Components

### Kubernetes Resources

```
k8s/gateway/04-backendpolicy-iap.yaml
├─ GCPBackendPolicy
│  ├─ targetRef: Service/agent-webapp
│  └─ spec.default.iap
│     ├─ enabled: true
│     ├─ oauth2ClientSecret: oauth-client-secret
│     └─ clientID: 449012790678-o4n...

k8s/overlays/prod/patches/agent-api-iap.yaml
├─ Environment Variables
│  ├─ ENABLE_IAP: "true"
│  ├─ LOG_IAP_HEADERS: "false"
│  ├─ DD_TRACE_CLIENT_IP_HEADER: "X-Forwarded-For"
│  └─ DD_TRACE_HEADER_TAGS: "X-Goog-Authenticated-User-Email:iap.user.email,..."

Kubernetes Secret: oauth-client-secret
├─ client_id: <OAuth 2.0 Client ID>
└─ client_secret: <OAuth 2.0 Client Secret>
```

### Application Code

```
packages/agent-api/src/middleware/iap-auth.ts
├─ extractIAPHeaders(req) → IAPUser | null
├─ extractIAPUser(req, res, next) → Middleware
├─ requireIAPAuth(req, res, next) → Middleware
├─ requireIAPDomain(domains[]) → Middleware
├─ getIAPUser(req) → IAPUser | null
└─ isIAPEnabled() → boolean

packages/agent-api/src/express-server.ts
├─ if (isIAPEnabled()) app.use(extractIAPUser)
└─ GET /api/me
   └─ Uses getIAPUser(req) for authentication
```

## Data Flow

```
1. User Clicks Login
   └─> Google OAuth Consent Screen

2. User Grants Permission
   └─> IAP Issues JWT Token

3. Request with Token
   └─> IAP Validates Token & IAM Permissions

4. Request Forwarded with Headers
   X-Goog-Authenticated-User-Email: accounts.google.com:user@example.com
   X-Goog-Authenticated-User-Id: accounts.google.com:123456789
   X-Goog-IAP-JWT-Assertion: eyJhbGc...
   X-Forwarded-For: 203.0.113.42

5. Middleware Extracts User
   email = "user@example.com"
   userId = "123456789"

6. Datadog Tags Added
   span.setTag('iap.user.email', 'user@example.com')
   span.setTag('usr.id', '123456789')

7. User Context Stored
   PostgreSQL: INSERT INTO users (id, ...) VALUES (hash(email), ...)

8. Trace Sent to Datadog
   APM Trace: service=agent-api, resource=POST /api/chat
              tags: {iap.user.email, usr.id, ...}
```

## Security Boundary

```
┌────────────────────────────────────────────────────────────┐
│                     Public Internet                         │
│            (Unauthenticated Users)                         │
└───────────────────────┬────────────────────────────────────┘
                        │
                        │ HTTPS
                        ↓
┌────────────────────────────────────────────────────────────┐
│              🔒 IAP Security Boundary 🔒                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ OAuth 2.0 Authentication                             │  │
│  │ IAM Authorization Check                              │  │
│  │ Only users with roles/iap.httpsResourceAccessor      │  │
│  └──────────────────────────────────────────────────────┘  │
└───────────────────────┬────────────────────────────────────┘
                        │
                        │ Authenticated Request + Headers
                        ↓
┌────────────────────────────────────────────────────────────┐
│              Protected Application Zone                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Gateway API                                          │  │
│  │ agent-webapp                                         │  │
│  │ agent-api                                            │  │
│  │ burger-api, burger-mcp                               │  │
│  │ PostgreSQL                                           │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

## Monitoring & Observability

```
Datadog Dashboard: IAP User Activity
┌─────────────────────────────────────────────────────────┐
│ Authenticated Requests (Last 24h)                       │
│ ████████████████████████████████████████ 1,234          │
│                                                          │
│ Unique Users                                             │
│ ██████████████████ 42 users                             │
│                                                          │
│ Top Users by Request Count                              │
│ 1. user1@example.com ████████████ 234 requests          │
│ 2. user2@example.com ███████ 156 requests               │
│ 3. user3@example.com ██████ 98 requests                 │
│                                                          │
│ Authentication Errors (401/403)                          │
│ ████ 12 errors                                          │
│                                                          │
│ Average Response Time (by user)                          │
│ All Users: 125ms                                        │
│ p50: 89ms | p95: 234ms | p99: 456ms                    │
└─────────────────────────────────────────────────────────┘
```
